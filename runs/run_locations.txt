
base results for stochastic algorithm (run on school power) are at:
nonparallel_gaussian_extra_seeds/stochastic_unrefined/combined_plan_time_results.yaml
parallel_gaussian_extra_seeds/stochastic_unrefined/combined_plan_time_results.yaml
the versions run on full power are at *_full_power/combined_0.1/
	full power was used for comp. time comparisons

deterministic runs for RSPECT are in deterministic_uniform
	(with comparison PROSPECT runs the standard ones above)

online planner results are in
runs/gaussian_online_extended and gaussian_online_extended_high_risk and gaussian_online_extended_infeasible
NOT runs\nonparallel_gaussian_extra_seeds\combined_results_for_execution_uniform_online\
bug at executeUtils 348 (jTour >= len() - 2 fixed to jTour >= len() - 1) creates *_v2 folders
	which affects only these runs as far as planning, because they are the only ones evaluating \hat{p}_r and \hat{\tau}(X) with actual runs dependent on online planning - tables for offline-online 0.1 and 0.5, and infeasible table (checked, generally increases tau)
	although \hat p_r also appears in terra, rspect tables and should be looked at (if printFailureRates was used). terra checked (minor diff, possibly typo), rspect-prospect checked (typos only)
	\hat p_r in refinement table is being pulled from offline predictions (because printMissionTimeComparison was used)

simulated annealing results in scripts/annealing
with our comparisons in runs/annealing_comparisons

terra results in scripts/terra
single_run is for one run per plan
multi_run incorrectly accounted for worst-case planning
multi_run_corrected (the results we use) are multiple runs per plan
runs/terra_comparison are stoch planner results

knapsack comparisons are in nonparallel_gaussian_knapsack

multiteam are in multiteam_uniform

ROS paper runs are in gazebo/launch/paper_case_high_speed_low_wind_v2 and _v2_extra_seeds

